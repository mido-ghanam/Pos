Django>=4.2,<5
djangorestframework>=3.14

# Token auth (DRF includes token support, but this adds JWT support if needed)
djangorestframework-simplejwt>=5.3

# Optional helpers
python-dotenv>=1.0
gunicorn>=20.1
requests>=2.30

