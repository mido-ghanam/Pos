# 🧪 Tests Folder - دليل الاختبار

## الملفات المتوفرة

### 1. `test_customers.py` - اختبار Customers API
اختبار شامل لجميع عمليات العملاء:
- ✅ تسجيل عميل جديد (2 مراحل)
- ✅ جلب جميع العملاء
- ✅ جلب تفاصيل عميل معين
- ✅ تحديث بيانات العميل (كامل وجزئي)
- ✅ البحث عن عملاء
- ✅ تصفية العملاء
- ✅ حجب عميل
- ✅ حذف عميل

### 2. `test_suppliers.py` - اختبار Suppliers API
اختبار شامل لجميع عمليات الموردين:
- ✅ تسجيل مورد جديد (2 مراحل)
- ✅ جلب جميع الموردين
- ✅ جلب تفاصيل مورد معين
- ✅ تحديث بيانات المورد (كامل وجزئي)
- ✅ البحث عن موردين
- ✅ تصفية الموردين
- ✅ تغيير حالة المورد (نشط/غير نشط)
- ✅ حذف مورد

---

## كيفية تشغيل الاختبارات

### متطلبات
```bash
pip install requests
```

### تشغيل اختبارات العملاء
```bash
python test_customers.py
```

### تشغيل اختبارات الموردين
```bash
python test_suppliers.py
```

---

## خطوات الاختبار بالتفصيل

### 1️⃣ بدء الخادم
```bash
python manage.py runserver
```

### 2️⃣ تشغيل الاختبار
```bash
# من داخل فولدر Tests
python test_customers.py
```

### 3️⃣ اتبع التعليمات
البرنامج سيطلب منك:
1. بيانات التسجيل (تسجيل خطوة 1)
2. OTP من WhatsApp (التحقق خطوة 2)
3. يقوم بباقي الاختبارات تلقائياً

---

## نتائج الاختبار

### ✅ نجاح
```
✅ PASS [POST] Register Customer Step 1
   URL: http://localhost:8000/partners/customers/register/
   Status: 201
   Response: {"status": true, "message": "OTP sent successfully"}
```

### ❌ فشل
```
❌ FAIL [POST] Register Customer Step 1
   URL: http://localhost:8000/partners/customers/register/
   Status: 400
   Response: {"status": false, "error": "Phone number already exists"}
```

---

## بيانات الاختبار الافتراضية

### Customers
```json
{
  "name": "محمد احمد",
  "phone": "201234567890",
  "email": "ahmed@example.com",
  "address": "القاهرة"
}
```

### Suppliers
```json
{
  "person_name": "علي احمد",
  "company_name": "شركة التوريد العربية",
  "phone": "201234567891",
  "email": "ali@supplier.com",
  "address": "الإسكندرية"
}
```

---

## عملية الاختبار

### اختبار Customers تشمل:

```
1. 📝 REGISTRATION TESTS
   - تسجيل جديد (الخطوة 1) - إرسال OTP
   - التحقق من OTP (الخطوة 2) - إنشاء الحساب

2. 📋 READ TESTS
   - جلب جميع العملاء
   - جلب عميل معين

3. ✏️ UPDATE TESTS
   - تحديث كامل البيانات
   - تحديث جزئي للبيانات

4. 🔍 SEARCH & FILTER TESTS
   - البحث بـ الاسم
   - التصفية حسب الحالة

5. 🚫 BLOCK TESTS
   - حجب العميل

6. 🗑️ DELETE TESTS
   - حذف العميل
```

### اختبار Suppliers تشمل:

```
1. 📝 REGISTRATION TESTS
   - تسجيل جديد (الخطوة 1) - إرسال OTP
   - التحقق من OTP (الخطوة 2) - إنشاء الحساب

2. 📋 READ TESTS
   - جلب جميع الموردين
   - جلب مورد معين

3. ✏️ UPDATE TESTS
   - تحديث كامل البيانات
   - تحديث جزئي للبيانات

4. 🔍 SEARCH & FILTER TESTS
   - البحث بـ الاسم
   - التصفية حسب الحالة

5. 🔄 TOGGLE TESTS
   - تغيير حالة المورد

6. 🗑️ DELETE TESTS
   - حذف المورد
```

---

## معالجة الأخطاء

### خطأ: "Connection refused"
```
❌ لم يتم العثور على الخادم
✅ الحل: تأكد من تشغيل Django:
   python manage.py runserver
```

### خطأ: "Invalid OTP"
```
❌ OTP غير صحيح
✅ الحل: 
   - تأكد من نسخ OTP بشكل صحيح من WhatsApp
   - OTP صالح لـ 10 دقائق فقط
```

### خطأ: "Phone already exists"
```
❌ رقم الهاتف موجود مسبقاً
✅ الحل: استخدم رقم هاتف جديد
```

---

## نصائح مهمة

1. **تأكد من الخادم:** الخادم يجب أن يكون يعمل قبل الاختبار
2. **OTP من WhatsApp:** تأكد من استقبال الرسالة
3. **بيانات صحيحة:** استخدم أرقام هاتف صحيحة
4. **Network:** تأكد من الاتصال بالإنترنت للـ WhatsApp API

---

## الملفات الإضافية

في نفس المجلد توجد ملفات اختبار أخرى:

| الملف | الغرض |
|------|-------|
| `auth/` | اختبارات Authentication |
| `Category/` | اختبارات Category |
| `Product/` | اختبارات Product |

---

## التشغيل المتقدم

### اختبار بـ بيانات مختلفة
عدل ملف الاختبار وغير `TEST_DATA`:

```python
TEST_DATA = {
    "step1_register": {
        "step": 1,
        "name": "اسمك الجديد",
        "phone": "رقم هاتفك",
        "email": "بريدك",
        "address": "عنوانك"
    },
    ...
}
```

### اختبار نقطة نهاية واحدة فقط
```python
# في الملف
if __name__ == "__main__":
    # شغل اختبار واحد فقط
    test_register_customer_step1()
```

---

## ملخص سريع

```bash
# 1. تأكد من الخادم
python manage.py runserver

# 2. من داخل Tests folder
cd Tests

# 3. شغل الاختبار
python test_customers.py  # أو test_suppliers.py

# 4. أدخل البيانات المطلوبة
# 5. شوف النتائج ✅
```

---

## الدعم

إذا واجهت مشكلة:
1. تحقق من أن الخادم يعمل
2. تحقق من صحة الـ OTP
3. تحقق من صحة أرقام الهاتف
4. شوف logs الخادم للمزيد من المعلومات

---

**Happy Testing! 🚀**

